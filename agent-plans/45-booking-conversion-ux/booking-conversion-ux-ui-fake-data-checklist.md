# UI Checklist (Fake Data) - Booking Conversion UX

**Scope:** UI-only work using placeholder data. No backend wiring required.

---

## Sticky CTA (Schedule)

- [ ] Create a reusable sticky CTA component (mobile only) for schedule pages.
- [ ] Use placeholder summary data (time + price) for layout validation.
- [ ] Ensure safe-area padding and no layout shift on scroll.

## Sticky CTA (Place Detail)

- [ ] Create a sticky CTA variant for place detail page.
- [ ] Use fake selection data for summary display.
- [ ] Confirm it does not overlap existing booking summary card.

## Auth Context Copy

- [ ] Add a small helper line in login/register layout (static text only).
- [ ] Keep copy visually subtle (muted text).

## Visual QA

- [ ] Mobile width (320px, 375px, 768px) checked.
- [ ] No horizontal scroll.
- [ ] Buttons are reachable and use teal for primary CTA.
