# Phase 3: QA

**Dependencies:** Phase 1 + Phase 2

---

## Manual QA

- Public schedule deep links
  - [ ] `/courts/:id/schedule?date=<day beyond max>` clamps to max day and clears selection.
  - [ ] `/courts/:id/schedule?month=<month beyond max>` clamps to max month.
  - [ ] When clamping happens, availability loads successfully (no error state).

- Error messaging
  - [ ] If availability query is rejected by booking window, show callout with "Jump to latest".
  - [ ] If a booking mutation is attempted beyond window (manual devtools / stale UI), toast message is friendly.

---

## Build Validation

```bash
pnpm lint
TZ=UTC pnpm build
```
