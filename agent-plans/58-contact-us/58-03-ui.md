# Phase 3: Public UI + CTA

**Dependencies:** Phase 2 complete  
**Parallelizable:** No  
**User Stories:** US-58-01

---

## Objective

Ship the public contact page, CTA in the footer, and redirect from `/contact` → `/contact-us`.

---

## Modules

### Module 3A: Contact page + CTA

**User Story:** `US-58-01`

#### UI Layout

```
┌─────────────────────────────────────────────┐
│ Contact Us                                  │
│ Short description                           │
│                                             │
│ ┌───────────────┐  ┌──────────────────────┐ │
│ │ Info panel    │  │ Form card            │ │
│ │ - response    │  │ Name, Email          │ │
│ │ - hours       │  │ Subject, Message     │ │
│ └───────────────┘  │ [Send message]       │ │
│                    └──────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Implementation Steps

1. Add `/contact-us` page with hero copy and contact form card.
2. Create `ContactUsForm` client component using StandardForm fields.
3. Add footer CTA button and update company link to `/contact-us`.
4. Add Next.js redirect for `/contact` → `/contact-us`.

#### Testing Checklist

- [ ] Form validation errors display inline
- [ ] Submit button shows loading state
- [ ] Success confirmation resets form
- [ ] Footer CTA routes correctly
- [ ] `/contact` redirects to `/contact-us`

---

## Phase Completion Checklist

- [ ] Contact page rendered in public layout
- [ ] Footer CTA live
