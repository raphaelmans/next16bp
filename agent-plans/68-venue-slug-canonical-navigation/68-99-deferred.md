# Deferred Work

Items explicitly out of scope for this plan.

---

## Deferred Features

| Feature | Priority | Reason Deferred |
|---------|----------|-----------------|
| Server-side canonical redirects for `/venues/[placeId]/schedule` and `/venues/[placeId]/book` | Medium | Current issue is detail-page flicker; these become low-risk once all entrypoints link slug-first |

---

## When to Revisit

- If users still hit UUID schedule/book URLs via external links or saved bookmarks.
