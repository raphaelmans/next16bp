# Phase 3: QA + Validation

**Dependencies:** Phase 2 complete
**Parallelizable:** No

---

## Objective

Confirm the flicker is gone and routing/canonical behavior is consistent.

---

## Manual QA

1. Discovery -> Detail
   - Open `/courts`
   - Click a place card
   - Confirm the first URL is `/venues/<slug>` (no UUID flash)

2. Direct UUID entrypoint
   - Open `/venues/<uuid>` directly
   - Confirm a server-side redirect to `/venues/<slug>` (no intermediate render)

3. Legacy / compatibility
   - Confirm `/places/...` continues to redirect to `/venues/...` via `src/proxy.ts`

---

## Commands

```bash
pnpm lint
TZ=UTC pnpm build
```

---

## Acceptance Criteria

- [ ] No visible UUID -> slug transition on venue detail
- [ ] `pnpm lint` passes
- [ ] `TZ=UTC pnpm build` passes
