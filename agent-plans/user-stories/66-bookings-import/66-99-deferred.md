# 66-99: Deferred Items - Bookings Import

## Overview

Items deferred from the 66-bookings-import domain to keep the initial scope focused on one-time AI normalization + review + commit.

---

## Deferred Items

| Feature | Reason | Notes |
|---------|--------|-------|
| Manual entry-only import (no file, no AI) | Discuss later | Reuse the same review/editor UI to manually create bookings rows |
| Multi-run AI normalization | Cost control | Could be enabled later via paid tier or support reset tooling |
| Imported bookings shown in owner "Reservations" inbox | Data model gap | Would require an external/guest booking concept instead of representing as availability exceptions |
