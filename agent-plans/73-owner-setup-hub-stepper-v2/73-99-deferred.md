# Stepper v2: Deferred Items

- Merge AddVenueCard + ClaimListingCard into a single combined card.
- Deep-link the hub to a specific step (e.g. `?step=verify`) and auto-focus that card.
- Improve venue selection when multiple venues exist (picker on the hub).
- Add “why not bookable?” checklist (verification + reservations enabled + courts + schedule/pricing) as a dedicated card.
