# Plan: Start OpenSpec Change (`schedule-pricing-addons`)

## Goal
Run the `/opsx-new` workflow for the confirmed change name `schedule-pricing-addons`, stop after showing first-artifact instructions, and do not create any artifacts beyond scaffold creation.

## Confirmed Inputs
- Change name: `schedule-pricing-addons` (confirmed by user)
- Schema: default (`spec-driven` from `openspec/config.yaml`), so omit `--schema`
- Context docs to read before drafting artifacts:
  - `docs/schedule-pricing-addons-erd.md`
  - `docs/schedule-pricing-addons-state-machine.md`
  - `docs/schedule-pricing-addons-v2-alignment-review.md`

## Execution Steps
1. Preflight checks (read-only):
   - Validate name is kebab-case.
   - Check whether `openspec/changes/schedule-pricing-addons/` already exists.
2. If change already exists:
   - Stop and suggest `/opsx-continue` instead of creating a duplicate change.
3. If change does not exist:
   - Run `openspec new change "schedule-pricing-addons"`.
4. Run `openspec status --change "schedule-pricing-addons"`.
5. From status output, identify the first artifact with status `ready`.
6. Run `openspec instructions <first-artifact-id> --change "schedule-pricing-addons"`.
7. Stop immediately (do not create any artifact files).

## Required Final Response Format
- Report change name and location: `openspec/changes/schedule-pricing-addons/`
- Report workflow/schema and artifact sequence from status output
- Report progress as `0/N artifacts complete`
- Include the first artifact template/instructions output
- End with:
  - `Ready to create the first artifact? Run /opsx-continue or just describe what this change is about and I'll draft it.`

## Critical Paths Affected
- New scaffold directory (only if creation succeeds):
  - `openspec/changes/schedule-pricing-addons/`
- No other repository files should be modified during this step.

## Verification
- Confirm duplicate handling works:
  - Existing change -> suggest `/opsx-continue`, no scaffold command run.
  - New change -> scaffold created once.
- Confirm status + instructions are shown and execution stops after first-artifact template.
- Confirm no artifact content is authored yet.
