Plan for admin courts/venues filters + delete

Context check
- In this repo, `src/app/(admin)/admin/courts/page.tsx` already renders `AdminPlacesList`, which includes a Featured filter and a Delete action.
- `src/app/(admin)/admin/venues/page.tsx` already uses the same `AdminPlacesList` for venues.

Proposed approach (if changes still desired)
1) Verify `AdminPlacesList` is wired for featured filtering and deletion
   - Featured filter: `featured` option in `useAdminCourts` maps to `trpc.admin.court.list` with `featured` boolean.
   - Delete action: `useDeleteAdminPlace` uses `trpc.admin.court.deletePlace`.
2) If courts page is not using `AdminPlacesList` in the target repo, replace the custom table with `AdminPlacesList` and pass the existing primary actions (batch add, add curated).
3) For venues, ensure `AdminPlacesList` is used (as already in this repo) and confirm entity label is "Venue" with locked type filter `reservable`.

Files that would be touched (if needed)
- `src/app/(admin)/admin/courts/page.tsx`
- `src/app/(admin)/admin/venues/page.tsx` (only if additional actions/labels requested)
- `src/features/admin/components/admin-places-list.tsx` (only if UI tweaks requested)

Verification
- Manual: open `/admin/courts` and confirm Featured filter appears, Delete action prompts with confirm input, and delete succeeds.
- Manual: open `/admin/venues` and confirm same dashboard behavior for venues.
