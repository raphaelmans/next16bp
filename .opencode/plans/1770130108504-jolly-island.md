# Stream Chat PoC (Provider Adapter + Auth + DM + Custom UI)

## Goal
Ship a minimal, gated Stream Chat proof-of-concept that:
- Uses an adapter/strategy provider layer so we can swap to a self-hosted provider later.
- Mints Stream user tokens server-side and ensures users exist.
- Creates/opens a deterministic 1:1 DM channel for two user IDs.
- Exposes a protected dashboard page that uses our own shadcn-based UI (no `stream-chat-react` UI kit / global CSS).

## Key Decisions
- API shape: use `tRPC chatPoc` (selected) to reuse existing auth context + typed hooks.

## Dependencies
- Keep: `stream-chat`
- Remove: `stream-chat-react` (we will not rely on the UI kit or its global CSS)
- Add/keep: AI Elements (already installed via `npx shadcn@latest add @ai-elements/all`)
  - UI building blocks live in `src/components/ai-elements/**`.

## Environment Wiring
Update:
- `.env.example`
- `src/lib/env/index.ts`

Add server vars (all optional so builds don’t break when PoC is unused):
- `CHAT_PROVIDER` (default `stream`)
- `CHAT_POC_ENABLED` (default disabled; prod requires `true`)
- `STREAM_CHAT_API_KEY`
- `STREAM_CHAT_API_SECRET`

Implementation notes:
- Parse `CHAT_POC_ENABLED` as `v === "true"` (don’t use `z.coerce.boolean()`; it treats "false" as truthy).
- Keep Stream keys optional in env schema; enforce presence at runtime when provider is `stream`.

## Backend Module: `src/lib/modules/chat/**`
Create a small provider abstraction + service.

Files (proposed):
- `src/lib/modules/chat/types.ts`
  - `ChatProviderId`, `ChatUser`, `ChatAuthResult`, `DmResult`
- `src/lib/modules/chat/providers/chat.provider.ts`
  - `IChatProvider` interface (provider-agnostic operations)
- `src/lib/modules/chat/providers/stream-chat.provider.ts`
  - `StreamChatProvider` implementation using `StreamChat` server client
- `src/lib/modules/chat/factories/chat.factory.ts`
  - `makeChatProvider()` (select by `env.CHAT_PROVIDER`)
  - `makeChatService()`
- `src/lib/modules/chat/services/chat.service.ts`
  - `ensureUser(...)`
  - `mintUserToken(userId)`
  - `getOrCreateDm({ userId, otherUserId })`
- `src/lib/modules/chat/helpers/dm-channel-id.ts`
  - Deterministic channel id helper (hash-based)

Deterministic DM channel id
- Don’t use `dm:${a}:${b}`:
  - `:` is risky/invalid in Stream channel IDs, and UUID-based IDs will exceed common 64-char limits.
- Use a stable hash: `dm-<sha256(min+":"+max).slice(0, 32)>`.

Stream provider behaviors (via `stream-chat` docs)
- Server client: `new StreamChat(apiKey, apiSecret, { disableCache: true })`
- Ensure user(s): `client.upsertUsers([{ id, name?, image? }, ...])`
- Mint token: `client.createToken(userId)`
- Create channel: `client.channel("messaging", channelId, { created_by_id: userId, members })` + `await channel.create()`

## API Layer (Recommended): tRPC
Create:
- `src/lib/modules/chat/chat-poc.router.ts`
  - `getAuth` (protected)
    - Guard: if prod and `env.CHAT_POC_ENABLED !== true` => throw tRPC NOT_FOUND with `NotFoundError` cause
    - Ensure current user exists in chat provider
    - Return `{ apiKey, user, token }`
  - `getOrCreateDm` (protected)
    - Input: `{ otherUserId: string }` (optionally `z.string().uuid()`)
    - Guard: same as above
    - Ensure both users exist + create DM channel
    - Return `{ channelId, memberIds }`

Wire into root:
- `src/lib/shared/infra/trpc/root.ts` add `chatPoc: chatPocRouter`

## UI (Custom: Stream JS + AI Elements)
Keep the existing page route, but replace Stream UI kit usage with AI Elements + shadcn primitives.

Route:
- `src/app/(protected)/dashboard/chat-poc/page.tsx`
  - Server component
  - Prod gate: if `process.env.NODE_ENV === "production" && env.CHAT_POC_ENABLED !== true` => `notFound()`
  - Render `<ChatPocClient />`

Remove Stream UI CSS import:
- Delete `src/app/(protected)/dashboard/chat-poc/layout.tsx` (or remove the `stream-chat-react` CSS import)

Use AI Elements components for the UI:
- Conversation scaffold: `src/components/ai-elements/conversation.tsx`
  - `Conversation`, `ConversationContent`, `ConversationEmptyState`, `ConversationScrollButton`
- Message primitives: `src/components/ai-elements/message.tsx`
  - `Message`, `MessageContent`, `MessageResponse` (markdown) (+ optional `MessageActions`)
- Composer: `src/components/ai-elements/prompt-input.tsx`
  - `PromptInput`, `PromptInputTextarea`, `PromptInputSubmit`, `usePromptInputAttachments`
- Attachments UI: `src/components/ai-elements/attachments.tsx`
  - `Attachments`, `Attachment`, `AttachmentPreview`, `AttachmentRemove`

Stream client + channel hooks (no `stream-chat-react`):
- Add `src/features/chat/hooks/useStreamClient.ts`
  - `StreamChat.getInstance(apiKey)` + `connectUser(user, token)` + cleanup `disconnectUser()`.
- Add `src/features/chat/hooks/useStreamChannel.ts`
  - `client.channel("messaging", channelId, { members })`
  - `channel.watch({ messages: { limit } })`
  - Subscribe to `message.new|updated|deleted|undeleted` via `channel.on(...)` and mirror `channel.state.messages` into React state.
  - Provide helpers:
    - `sendText(text)` -> `channel.sendMessage({ text })`
    - `sendFiles(files)` -> `channel.sendFile(file)` + `channel.sendMessage({ attachments })`
    - `loadMore()` -> `channel.query({ messages: { id_lt } })`
    - `markRead()` -> `channel.markRead()`

Client component rewrite:
- Update `src/features/chat/components/ChatPocClient.tsx`
  - Keep tRPC calls: `trpc.chatPoc.getAuth` + `trpc.chatPoc.getOrCreateDm`.
  - Use `useStreamClient` + `useStreamChannel` for state.
  - Render messages with AI Elements:
    - `Conversation` / `ConversationContent` for scroll container
    - Map Stream message -> `Message from={isMine ? "user" : "assistant"}`
    - Render text as `<MessageResponse>{message.text}</MessageResponse>`
    - Render Stream attachments by mapping to AI Elements `AttachmentData` and using `Attachments`.
  - Composer uses `PromptInput` / `PromptInputTextarea` / `PromptInputSubmit`.
    - Implement `onSubmit` to call `sendText` and/or upload files then send attachment message.
    - Show composer attachments using `usePromptInputAttachments()` + `Attachments` + `AttachmentRemove`.

## Validation
1) Fix pnpm install if needed (node_modules/store mismatch):
   - `pnpm install` (if it fails, we’ll follow pnpm’s recommended cleanup; typically remove `node_modules` and reinstall).
2) `pnpm lint`
3) `TZ=UTC pnpm build`

Manual smoke test
- Set env vars (Stream key/secret) in `.env.local`
- Start dev: `pnpm dev`
- Navigate to `/dashboard/chat-poc` while logged in
- Enter a second valid userId, create DM, send a message, reload page, ensure DM opens again

Extra UI smoke checks
- Open the page without `stream-chat-react` installed; it should still compile (no imports).
- Verify message list updates live on `message.new` events (two browser sessions).
- Verify load older messages works (if implemented).

---

## Next: Productize For Reservations (V1)

### Goals
- Enable peer-to-peer chat between the **player** and **court owner** for a given reservation.
- Enable chat from `CREATED` through the active lifecycle (player UI can still auto-open only on `CONFIRMED`).
- Ensure future complaint/dispute workflows can reliably retrieve **auditable message evidence**.
- Keep provider adapter/strategy so we can swap Stream → self-host later.

### V1 Scope (Simple, Extensible)
- Channel model: **1 channel per reservation** (recommended for auditability).
- Channel becomes available when reservation `status ∈ { CREATED, AWAITING_PAYMENT, PAYMENT_MARKED_BY_USER, CONFIRMED }`.
- Persist a small mapping in DB: reservation ↔ provider channel.
- Add admin-only ability to capture a **transcript snapshot** (immutable evidence) stored in DB.

Non-goals for V1 (defer)
- Full dispute/complaint workflow UI.
- Stream webhook ingestion of every message event (v2).
- Guest booking chat (`guestProfileId` reservations) (v2).

### Reservation + Participants Mapping
Source of truth:
- Reservation status contract: `docs/reservation-state-machine-level-2-engineering.md`
- Owner acceptance/confirm flows:
  - `src/lib/modules/reservation/services/reservation-owner.service.ts`
    - `acceptReservation(...)`: paid -> `AWAITING_PAYMENT`, free -> `CONFIRMED`
    - `confirmPayment(...)`: `PAYMENT_MARKED_BY_USER` -> `CONFIRMED`

Participants (auth user ids):
- **Player**: `reservation.playerId -> profile.userId` (if `playerId` is null, reservation is guest booking; v1: no chat)
- **Owner**: `reservation.courtId -> court.placeId -> place.organizationId -> organization.ownerUserId`

### Channel Model
- Provider type: `messaging`
- Deterministic channel id: `res-${reservationId}`
- Channel custom data (provider-specific): `{ reservation_id: reservationId }`

### DB Schema (Minimal)
Add two tables to support linkage + audit snapshots.

1) `reservation_chat_thread`
- Purpose: map reservation → provider channel.
- Columns (suggested):
  - `id` uuid PK
  - `reservation_id` uuid FK (unique) -> `reservation.id`
  - `provider_id` (text/varchar; e.g. `stream`)
  - `provider_channel_type` (text; `messaging`)
  - `provider_channel_id` (text; `res-<uuid>`)
  - `created_by_user_id` uuid FK -> `auth.users.id` (nullable)
  - `created_at` timestamptz default now
- Constraints/indices:
  - `unique(reservation_id)`
  - `unique(provider_id, provider_channel_type, provider_channel_id)`

2) `reservation_chat_transcript`
- Purpose: immutable “evidence capture” snapshots for disputes/complaints.
- Columns (suggested):
  - `id` uuid PK
  - `reservation_id` uuid FK -> `reservation.id`
  - `provider_id`, `provider_channel_type`, `provider_channel_id`
  - `captured_by_user_id` uuid FK -> `auth.users.id` (nullable)
  - `captured_at` timestamptz default now
  - `message_count` int
  - `first_message_at`, `last_message_at` timestamptz nullable
  - `transcript_sha256` varchar(64)
  - `transcript_json` jsonb
- Indices:
  - `(reservation_id, captured_at)`

Files:
- New schema: `src/lib/shared/infra/db/schema/reservation-chat.ts`
- Export: `src/lib/shared/infra/db/schema/index.ts`
- New migration: `drizzle/0020_reservation_chat.sql` (next available number)

### Backend (tRPC)
Add a reservation-aware router separate from `chatPoc`.

Router:
- `src/lib/modules/chat/reservation-chat.router.ts`
- Wire: `src/lib/shared/infra/trpc/root.ts` as `reservationChat`

Procedures:
1) `reservationChat.getSession`
- Input: `{ reservationId }`
- Output: `{ auth: { apiKey, user, token }, channel: { providerId, channelType, channelId, memberIds } }`
- Behavior:
  - Resolve reservation + participants
  - Gate by status (CREATED | AWAITING_PAYMENT | PAYMENT_MARKED_BY_USER | CONFIRMED)
  - Ensure caller is participant (player or owner)
  - Ensure Stream users exist
  - Ensure channel exists (idempotent)
  - Upsert `reservation_chat_thread` mapping (idempotent)

2) `reservationChat.captureTranscriptSnapshot` (admin-only)
- Input: `{ reservationId }`
- Behavior:
  - Look up thread mapping; export all messages from provider
  - Canonicalize JSON + compute sha256
  - Insert row into `reservation_chat_transcript` (append-only)

3) `reservationChat.listTranscriptSnapshots` (admin-only)
- Input: `{ reservationId }`
- Output: snapshot metadata list

Service/repository layering (match conventions like claim/verification):
- `src/lib/modules/chat/services/reservation-chat.service.ts`
- `src/lib/modules/chat/repositories/reservation-chat-thread.repository.ts`
- `src/lib/modules/chat/repositories/reservation-chat-transcript.repository.ts`

Provider abstraction extensions (keep strategy pattern):
- `src/lib/modules/chat/providers/chat.provider.ts`
  - Add: `ensureReservationChannel(...)`
  - Add: `exportChannelMessages(...)` (for snapshots)
- `src/lib/modules/chat/providers/stream-chat.provider.ts` implement
- `src/lib/modules/chat/services/chat.service.ts` can stay for DM; add reservation-focused service separately.

### UI Integration (V1)
- Player reservation detail:
  - `src/app/(auth)/reservations/[id]/page.tsx`
- Player payment page (helpful during AWAITING_PAYMENT / PAYMENT_MARKED_BY_USER):
  - `src/app/(auth)/reservations/[id]/payment/page.tsx`
- Owner reservation detail:
  - `src/app/(owner)/owner/reservations/[id]/page.tsx`

Add a reusable component:
- `src/features/chat/components/ReservationChatClient.tsx`
  - Calls `trpc.reservationChat.getSession`
  - Reuses Stream hooks + AI Elements UI patterns from PoC
  - Chat is available in `CREATED`, but player UI should remain subtle (no auto-open); auto-open only on `CONFIRMED`

### Auditability + Dispute Readiness
V1: “snapshot on demand”
- Admin captures transcript snapshot when a complaint/dispute is created.
- Snapshot stored in DB as immutable JSON with sha256.

V2: webhook ingestion
- Add `/api/webhooks/stream-chat` and store append-only message events in DB for full compliance and independence from provider retention.

### Verification
- Unit-ish: existing lint/build pipeline.
- Manual:
  - Create reservation (player) → as owner accept (paid: AWAITING_PAYMENT or free: CONFIRMED)
  - Both parties see chat appear on their reservation detail pages
  - Send messages + attachments
  - Verify access control by trying with a non-participant user
  - As admin, capture a transcript snapshot and verify it persists

---

## UI Upgrade: Floating Chat Widgets (Player + Owner)

### UX Goals
- Avoid “in-layout wall of chat”; chat should feel like a **support widget**.
- Player: when reservation becomes **CONFIRMED**, show the chat entry and **auto-expand once**.
- Owner: show a persistent chat icon across **all owner routes** with an inbox-style list (latest message + unread).

### Design Direction (KudosCourts)
- Follow `business-contexts/kudoscourts-design-system.md`:
  - Minimalist bento, warm neutrals, subtle borders/shadows.
  - Teal (`primary`) for the main chat CTA; orange for highlights; red only for destructive.
- Panel should feel like a compact “card stack”:
  - Rounded-lg/xl, border, soft shadow, dense but readable typography.
  - Motion: 150–250ms open/close + subtle badge pulse for unread.

### Player Widget
Where:
- Replace the embedded chat card in `src/app/(auth)/reservations/[id]/page.tsx`.

Behavior:
- Chat trigger shows when reservation status is eligible.
- When status becomes `CONFIRMED`, auto-open the widget once (store `sessionStorage["chat:autoOpen:res:<id>"]=true`).

Implementation:
- Refactor `src/features/chat/components/ReservationChatClient.tsx` into:
  - `src/features/chat/components/chat-thread/ReservationChatThread.tsx` (thread UI only)
  - `src/features/chat/components/chat-widget/PlayerReservationChatWidget.tsx` (floating button + panel + auto-open)
- Use shadcn primitives:
  - `Button`, `Badge`, `Card`, `ScrollArea`, `Separator`, `Tooltip` (optional)
- Panel (desktop): fixed bottom-right, size ~ `w-[380px] h-[560px]`.
- Panel (mobile): render as `Drawer` or a full-height “sheet-like” panel.

### Owner Widget (Global Inbox)
Where:
- Mount once in `src/app/(owner)/layout.tsx` as a client component so it persists across owner navigation.

Behavior:
- Always show floating icon (assistant/support style).
- When opened:
  - Left: channel list (“Inbox”) sorted by last message.
  - Right: active thread.
  - Mobile: list-first, then thread (back button).
- Badge: show unread count (from Stream channel unread count) when available.

Data strategy (keep v1 simple):
- Add a non-PoC auth endpoint to connect Stream client without a reservationId:
  - New router `src/lib/modules/chat/chat.router.ts` (or add `getAuth` to `reservationChatRouter`)
  - Procedure: `getAuth -> { apiKey, user, token }` using existing provider + `ChatService`.
- Channel list:
  - Client calls `streamClient.queryChannels({ type: "messaging", members: { $in: [ownerUserId] } }, { last_message_at: -1 }, { limit: 30 })`.
  - Filter to reservation channels by:
    - `channel.id` prefix `res-` and/or `channel.data.reservation_id`.

Important collision note:
- Owner pages may already render `ReservationAlertsPanel` (fixed/draggable z-50). Avoid overlap by:
  - Defaulting chat button to `bottom-6 right-6`, and shifting alerts button when minimized, OR
  - Creating a `FloatingDock` wrapper that vertically stacks multiple floating buttons.

Suggested components:
- `src/features/chat/components/chat-widget/OwnerChatWidget.tsx`
- `src/features/chat/components/chat-widget/OwnerInboxList.tsx`
- `src/features/chat/components/chat-widget/OwnerInboxItem.tsx`
- Reuse `ReservationChatThread` for the thread view.

### Eligibility / Gating
- Selected: enable reservation chat from `CREATED` for both participants.
- Player UX: auto-open only when reservation becomes `CONFIRMED`.

Implementation hooks:
- Update `src/lib/modules/chat/services/reservation-chat.service.ts` status gate to include `CREATED`.
- Ensure the reservation channel exists early so the owner inbox can show new requests:
  - Best-effort `ensureReservationChannel` after reservation creation in:
    - `src/lib/modules/reservation/services/reservation.service.ts` (`createReservationForCourt`, `createReservationForAnyCourt`)
  - Do not fail reservation creation if chat provider is down (log + retry on first open).

### Verification
- Player:
  - Confirmed reservation auto-opens chat once.
  - Refresh: widget doesn’t auto-open again.
- Player (visibility fix):
  - Widget button shows for active reservation statuses (including `CREATED`).
  - In `CREATED`, sheet shows “Waiting for owner confirmation” messaging (and optionally allows sending).
- Owner:
  - Widget persists across owner routes.
  - New reservation shows up in list (if channel exists) and unread badge updates.
- a11y:
  - Floating trigger reachable via keyboard; focus trapped inside panel; ESC closes.

### Player Widget Visibility (Follow-up)
Problem:
- Current player widget only renders for `CONFIRMED`, so players won’t see it while waiting.

Change:
- Show player widget button for reservation statuses:
  - `CREATED`, `AWAITING_PAYMENT`, `PAYMENT_MARKED_BY_USER`, `CONFIRMED`
- Keep auto-open behavior ONLY when status becomes `CONFIRMED`.
- In non-confirmed states, show a small status message in the sheet header:
  - `CREATED`: “Waiting for owner confirmation”
  - `AWAITING_PAYMENT`: “Owner accepted — awaiting payment”
  - `PAYMENT_MARKED_BY_USER`: “Waiting for owner confirmation”

Files:
- Update `src/features/chat/components/chat-widget/player-reservation-chat-widget.tsx`
  - Render widget for all active statuses.
  - Enable `trpc.reservationChat.getSession` for those statuses (not only `CONFIRMED`).
  - Disable auto-open unless `CONFIRMED`.
