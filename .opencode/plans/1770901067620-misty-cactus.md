# Plan: Availability Studio court loading cue

## Goal
Add a visible loading indicator to the Court selector while courts are loading, without changing the existing empty state alert.

## Files to update
- `src/app/(owner)/owner/bookings/page.tsx`

## Plan
1. Add `Loader2` to the lucide-react icon imports in `src/app/(owner)/owner/bookings/page.tsx`.
2. Update the Court `SelectTrigger` to include a loading spinner when `courtsLoading` is true:
   - Add a `className` on `SelectTrigger` (e.g. `relative pr-9`) to make room for the spinner.
   - Render a `Loader2` icon inside the trigger with `animate-spin`, `text-muted-foreground`, and `pointer-events-none`.
   - Use a dynamic placeholder: "Loading courts..." while loading, otherwise "Select a court".
3. Keep the existing empty alert and all other behavior unchanged.

## Verification
- Manual: open Availability Studio, pick a venue, confirm the Court select shows a spinner and “Loading courts...” while loading.
- Optional: run `pnpm lint`.
