# Docs: Place Detail Migration Status

## Context
- The client conventions migration plan targets `src/app/(public)/places/[placeId]/place-detail-client.tsx` for refactor.
- In the codebase, that file still exists (and is large), but the core logic was already extracted into the discovery feature (`src/features/discovery/hooks.ts`, `src/features/discovery/helpers.ts`) and it leans on `src/common/*` utilities.
- The migration docs mark the “Public place detail refactor” as completed, but `agent-plans/80-client-conventions-migration/client-conventions-migration-client-dev1-checklist.md` still has the corresponding checkbox unchecked, which makes it look like the file was never migrated.

## Goal
- Reconcile documentation so all migration artifacts consistently reflect the current (accepted) state.

## Scope
- Docs only (no code changes).

## Planned Changes
1. Update `agent-plans/80-client-conventions-migration/client-conventions-migration-client-dev1-checklist.md`
   - Mark “Split place detail into sections” as complete.
   - Add a short note clarifying what “done” means here: logic moved to `src/features/discovery/*`; the route-level client file remains as the orchestration layer.

2. Optional: add a one-liner clarification to one of these to eliminate ambiguity for future readers:
   - `agent-plans/80-client-conventions-migration/80-03-targeted-refactors.md` (place detail section), or
   - `agent-plans/80-client-conventions-migration/80-00-overview.md` (Phase 3 completion note)

## Verification
- Search in `agent-plans/80-client-conventions-migration/` to confirm the place detail item is consistently marked complete.
- Re-read the updated checklist section to ensure it matches the repo state.
