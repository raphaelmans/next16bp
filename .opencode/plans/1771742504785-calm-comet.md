# Chat Badge Investigation Plan (raphael@rethndr.com)

## Objective
- Investigate why chat badge shows `9` while chat appears empty, using Supabase MCP and code path correlation.
- Deliver a root-cause classification and a minimal, low-risk remediation recommendation.

## Confirmed Context (already validated)
- Account identity and role:
  - `auth.users.id = 3f6c6a62-5570-4639-a8eb-bac96b037f01`
  - `email = raphael@rethndr.com`
  - `public.user_roles.role = admin`
- Secondary Raphael account exists (`raphael@kudoscourts.com`, role `member`), so account/session confusion is possible and should be checked when reproducing.
- Reservation inbox data for `raphael@rethndr.com`:
  - `reservation_chat_thread` rows where user is reservation participant (player or owner): `8`
  - Service visibility rule equivalent result: `visible_threads = 0`, `hidden_threads = 8`
  - Hidden statuses are `CONFIRMED` (ended) and `EXPIRED`
- `chat_inbox_archive` has no rows for this user, so this is not caused by manual archive state.
- Frontend code mismatch is present:
  - Badge unread sum includes all reservation channels from Stream (`channel.state.unreadCount`)
  - Conversation list only renders reservations returned by `getThreadMetas(... includeArchived: false)` (status/time filtered)
  - Result can be badge > 0 while list is empty.

## Recommended Investigation Execution
1. Re-confirm account context for reproduction
   - Validate active session identity in app and Supabase auth logs (`/auth/v1/user`, token refresh events) to avoid cross-account misread.

2. Re-run focused Supabase SQL audit (saved queries)
   - User + role checks (`auth.users`, `public.user_roles`, `public.profile`)
   - Reservation thread mapping and visibility rule audit (`reservation_chat_thread` + reservation joins)
   - Archive-state audit (`chat_inbox_archive`) to exclude hidden-by-archive.

3. Correlate with code behavior
   - Confirm badge source: `src/features/chat/components/chat-widget/reservation-inbox-widget.tsx`
   - Confirm visibility filter source: `src/lib/modules/chat/services/reservation-chat.service.ts#getThreadMetas`
   - Confirm read-side behavior: `src/features/chat/components/chat-thread/stream-chat-thread.tsx` and `src/features/chat/hooks/useStreamChannel.ts`

4. Classify root cause for this report
   - Primary classification expected: `visibility-filter mismatch` (unread counted for threads filtered out of list).
   - Secondary check: account-switch noise (same person has two accounts) if reproduction disagrees.

5. Prepare remediation proposal
   - Operational: none required for archive cleanup (no archived rows).
   - Code-level recommendation (if approved): align badge count with same visible set used by list, or explicitly segment hidden/history unread from active inbox unread.

## Critical Files (for follow-up fix)
- `src/features/chat/components/chat-widget/reservation-inbox-widget.tsx`
- `src/lib/modules/chat/services/reservation-chat.service.ts`
- `src/features/chat/domain.ts`
- `src/features/chat/components/chat-thread/stream-chat-thread.tsx`

## Verification
- Supabase MCP verification:
  - Re-run SQL and confirm `total_threads=8`, `visible_threads=0`, `hidden_threads=8`, `chat_inbox_archive=0` for `raphael@rethndr.com`.
  - Confirm no DB-side evidence of archive-caused hiding.
- UI verification (manual smoke):
  - Sign in as `raphael@rethndr.com` in owner/player portal.
  - Confirm badge can remain > 0 while list is empty due to status filtering.
  - If fix is implemented later, verify badge parity with visible list and no regression in support inbox behavior.
- If code changes are made in follow-up, run `pnpm lint` and repeat manual smoke matrix.
